Big picture

After introducing BaseMultiplePropertyOrchestrator<TContext>, the split should be:

✅ Base orchestrator handles (generic / reusable):

Stuff that is the same across any property product:

Template validation flow (existence, worksheet checks, etc.)
Extracting rows from Excel
Orchestration pipeline:
Extract rows
Build domain objects
Run validation
Run geolocation
Call prior submit
Common geolocation workflow:
Call geolocation service
Populate lat/lng
Handle overridden coordinates logic
Calling APIs:
Country API
Location API
Returning TemplateUploadResult

👉 Think: “framework” / pipeline

🔥 What TerrorismOrchestrator handles (product-specific)

Everything here is specific to the terrorism insurance product rules and template structure.

1. 📄 Template structure (3 sheets)

Terrorism has a unique Excel shape:

Sheet 1 → Locations
Sheet 2 → Floating values
Sheet 3 → First loss limit
TemplateConfig.GetFloatingValuesColumnConfiguration()
TemplateConfig.GetFirstLossLimitColumnConfiguration()

👉 This is NOT generic — other products may:

Have different sheets
No floating values
Different layouts
2. 💰 Financial rules (TIV logic)
_maximumLocationTivValue = 300000000;

Rules like:

Max £300M per location
Combine:
TIV (Total Insured Value)
Floating values
First loss limit logic
if ((propertyLimit.TotalInsuredValue + propertyTotalFloatingValues) > _maximumLocationTivValue ...)

👉 This is pure business logic specific to terrorism underwriting

3. 🏙️ Zone A postcode validation (VERY domain-specific)
IsZoneAAddress(...)
UK-only logic
London Zone A postcode detection
Rooftop precision requirement
if (isZoneA && isNotRooftopPrecision && !hasSpreadsheetSuppliedLatitudeAndLongitude)

👉 This is:

Geography-specific
Product-specific
Regulatory/underwriting-driven

No other product will reuse this.

4. 🏢 Building use validation
BuildingUseTypes
Valid enum values
Case-insensitive handling
Throws if invalid

👉 This is tied to:

Terrorism risk modelling
Not a generic location concept
5. 📊 Floating values logic
FloatingValuesHasCover(...)
SetExtractedPropertyFloatingValues(...)
Contents
Loss of rent
ICOW
Alternative accommodation

👉 This structure is:

Specific to terrorism product pricing
Not universal across products
6. 🧾 “No cover” validation
"No cover has been provided for this location"

Triggered when:

No floating values
TIV == 0

👉 Again: product rule, not platform rule

7. 🔁 Duplicate address logic
IdentifyDuplicateAddressKeys(...)
Address1 + Postcode uniqueness rule

👉 This could be generic, but:

Currently tied to terrorism behaviour
Might differ per product
8. 🧩 How PropertyLimit is built
new PropertyLimit
{
    PropertyDamageLimit = ...
    ContentsDamageLimit = ...
    ...
}

👉 Mapping from spreadsheet → domain model is:

Template-specific
Product-specific
🧠 The clean mental model
Base class = “HOW”

How do we process a spreadsheet?

Extract
Validate
Geolocate
Return result
Terrorism orchestrator = “WHAT”

What does terrorism actually mean?

What columns exist
What rules apply
What validations matter
What constitutes an error
💬 One-liner you can use in standup/review

“The base orchestrator now handles the generic pipeline — extraction, validation, geolocation — while the TerrorismOrchestrator focuses purely on terrorism-specific rules like Zone A validation, TIV limits, and the multi-sheet template structure.”
