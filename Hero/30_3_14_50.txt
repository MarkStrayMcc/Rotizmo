public static PropertyLimit CreatePropertyLimit(string address1, string postcode, int rowNumber)
{
    return new PropertyLimit
    {
        RowNumber = rowNumber,
        InsuredAddress = new Hero.Models.ClientLocation
        {
            Address1 = address1,
            Address2 = "",
            City = "London",
            County = "",
            Postcode = postcode,
            CountryId = 1,
            ClientLocationProperties = new List<WebApiDto.Dto.ClientLocationProperties>()
        }
    };
}

_subject.PropertyLimitsToReturn = new List<PropertyLimit>
{
    TestMultiplePropertyOrchestrator.CreatePropertyLimit("10 High Street", "SW1A 1AA", 2)
};

_workbook?.Dispose();
